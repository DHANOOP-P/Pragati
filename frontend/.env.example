# Leave empty locally — Vite proxies /api to http://localhost:5000
# Production (Vercel): the public URL of the hosted Express API, including /api
# VITE_API_URL=https://your-app.fly.dev/api
VITE_API_URL=
